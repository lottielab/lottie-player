import React from 'react';

export const InteractiveLottieExample = () => {
  // This is a pure HTML example, so we just return an iframe showing the HTML page
  return <iframe src="./examples/interactive-lottie.html" style={{ width: '100%', height: '1200px' }} />;
};
